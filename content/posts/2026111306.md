---author: "Michael Henderson"
date: 2026-11-13
linktitle: "不看血亏！o3国内接入Python示例 2026最新低成本接入方案，附10个即用Python示例脚本"
menu:
  main:
    parent: tutorials
next: /posts/lez
prev: /posts/beq
title: "不看血亏！o3国内接入Python示例 2026最新低成本接入方案，附10个即用Python示例脚本"
weight: 1
tags: ["O3模型", "Claude", "Gemini"]
---

# 不看血亏！o3国内接入Python示例 2026最新低成本接入方案，附10个即用Python示例脚本

说实话，国内开发者想用上最新的o3模型，这件事本身就挺折腾的——o3作为OpenAI最新推理模型，性能确实炸裂，但翻墙、绑卡、防封号，一套流程下来，代码还没跑通，人先累趴下了。

最近一段时间用下来，[千聚api聚合站](https://www.qianjuai.com/)（www.qianjuai.com）让我省了不少事。接入o3模型，一行代码都不用改多，把base_url换成千聚的，国内网络环境直接跑通。今天这篇，就是要把o3+Python接入的最低成本方案、价格坑位、以及10个即用Python示例脚本全部拆解清楚。



👉 [立即注册千聚api聚合站，新用户送 $0.2 消费额度](https://www.qianjuai.com/register)

## o3模型国内接入为什么选它

[千聚api聚合站](https://www.qianjuai.com/)是一个国内可直连的AI大模型API中转聚合平台，你不用翻墙、不用绑海外信用卡、不用注册一堆麻烦的OpenAI账号。它的接口格式完全兼容OpenAI标准，这就意味着：你之前写好的任何OpenAI Python代码，只需要把`base_url`那一行改一下，国内的网络环境就能直接跑o3模型。

o3模型和o1模型一样，属于OpenAI的最新推理系列，在数学、编程、科学推理上表现非常亮眼。[千聚api聚合站](https://www.qianjuai.com/)不仅支持o3系列，还有500+模型供你切换，对于经常需要对比不同模型效果的人来说，省下来的时间非常可观。

---

## 价格怎么算——1元买1美元Token

[千聚api聚合站](https://www.qianjuai.com/)的定价策略特别清晰：**1元人民币 = 1美元Token额度，按OpenAI官方价格1:1计费。**

o3模型的官方价格通常比GPT-4o略高，因为它是推理模型，算力消耗更大。但通过[千聚api聚合站](https://www.qianjuai.com/)的聚合渠道，你可以用人民币直接支付，不需要承担汇率波动和海外银行手续费。

| 分组名称 | 费率倍数 | 说明 |
| --- | --- | --- |
| 默认（混合） | 官方×1 | 国内直连，包含o1、o3、GPT-4o等主力模型 |
| 官转OpenAI | 官方×3 | 官方高可靠渠道，适合生产环境 |
| 限时特价 | 官方×0.6 | DeepSeek、Gemini、国产模型专属，不支持o3 |

大多数开发者用默认分组就够了，1元人民币当1美元用，o3和o1都支持。最低1元起充，不用一次性压几百块进去试错，这个门槛对个人开发者来说非常友好。

---

## 接入o3 Python示例有多简单

真的只是改一行代码的事：

python
# 原来的OpenAI接入写法
from openai import OpenAI

client = OpenAI(
    base_url="https://api.openai.com/v1",  # 以前这行需要翻墙
    api_key="你的openai_key"
)

# 换成[千聚api聚合站](https://www.qianjuai.com/)的写法
client = OpenAI(
    base_url="https://www.qianjuai.com/v1",  # 国内直连，不必翻墙
    api_key="你在千聚申请的key"
)

# 调用o3模型
response = client.chat.completions.create(
    model="o3-mini",    # o3-mini性价比最高
    messages=[
        {"role": "user", "content": "用Python写一个快速排序算法"}
    ]
)

print(response.choices[0].message.content)


## 10个即用Python示例脚本

下面的10个脚本可以直接复制使用，覆盖了o3模型的常见使用场景。请确保你已经替换了API base_url和API key。

### 示例1：基础对话（o3-mini）
python
from openai import OpenAI

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
response = client.chat.completions.create(
    model="o3-mini",
    messages=[{"role": "user", "content": "解释什么是量子纠缠"}]
)
print(response.choices[0].message.content)


### 示例2：流式输出（打字效果）
python
from openai import OpenAI

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
stream = client.chat.completions.create(
    model="o3-mini",
    messages=[{"role": "user", "content": "写一篇200字的小故事"}],
    stream=True
)
for chunk in stream:
    print(chunk.choices[0].delta.content or "", end="")


### 示例3：代码审查
python
from openai import OpenAI

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
code = """
def add(a,b):
    return a+b
"""
response = client.chat.completions.create(
    model="o3-mini",
    messages=[{"role": "user", "content": f"审查以下代码的安全性并给出优化建议：{code}"}]
)
print(response.choices[0].message.content)


### 示例4：多轮对话（记忆上下文）
python
from openai import OpenAI

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
messages = [
    {"role": "system", "content": "你是一个Python编程导师"},
    {"role": "user", "content": "如何定义一个类？"},
    {"role": "assistant", "content": "使用`class`关键字定义类。"},
    {"role": "user", "content": "给我一个例子"}
]
response = client.chat.completions.create(
    model="o3-mini",
    messages=messages
)
print(response.choices[0].message.content)


### 示例5：JSON结构化输出
python
from openai import OpenAI

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
response = client.chat.completions.create(
    model="o3-mini",
    messages=[{"role": "user", "content": "列出5种Python数据结构，返回JSON格式"}],
    response_format={"type": "json_object"}
)
print(response.choices[0].message.content)


### 示例6：利用o3推理能力解决数学题
python
from openai import OpenAI

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
response = client.chat.completions.create(
    model="o3-mini",
    messages=[{"role": "user", "content": "\
一个水池，单开进水管5小时注满，单开出水管8小时排空。同时开进水和出水，几小时能注满？"}]
)
print(response.choices[0].message.content)


### 示例7：图片识别（Base64格式，o3支持视觉）
python
from openai import OpenAI
import base64

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
with open("chart.png", "rb") as f:
    image_data = base64.b64encode(f.read()).decode("utf-8")

response = client.chat.completions.create(
    model="o3-mini",
    messages=[
        {"role": "user", "content": [
            {"type": "text", "text": "分析图表并总结关键数据"},
            {"type": "image_url", "image_url": {"url": f"data:image/png;base64,{image_data}"}}
        ]}
    ]
)
print(response.choices[0].message.content)


### 示例8：调用o3高精度模式（更长推理时间）
python
from openai import OpenAI

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
response = client.chat.completions.create(
    model="o3-mini",
    reasoning_effort="high",  # 高推理模式，精度更高但响应稍慢
    messages=[{"role": "user", "content": "证明√2是无理数"}]
)
print(response.choices[0].message.content)


### 示例9：批量处理（for循环）
python
from openai import OpenAI

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
questions = ["Python中list和tuple的区别", "什么是装饰器", "解释生成器"]
for q in questions:
    response = client.chat.completions.create(
        model="o3-mini",
        messages=[{"role": "user", "content": q}]
    )
    print(f"问题：{q}\n答案：{response.choices[0].message.content}\n---")


### 示例10：使用工具调用（Function Calling）
python
from openai import OpenAI

client = OpenAI(base_url="https://www.qianjuai.com/v1", api_key="your_key")
functions = [
    {
        "name": "get_weather",
        "description": "获取指定城市的天气",
        "parameters": {
            "type": "object",
            "properties": {
                "city": {"type": "string", "description": "城市名"}
            },
            "required": ["city"]
        }
    }
]
response = client.chat.completions.create(
    model="o3-mini",
    messages=[{"role": "user", "content": "北京今天天气怎么样？"}],
    functions=functions,
    function_call="auto"
)
print(response.choices[0].message)


---

## 额外福利：不只是o3

[千聚api聚合站](https://www.qianjuai.com/)支持的模型远不止o3系列。OpenAI的GPT-4o、GPT-4o-mini、o1、o1-mini、文生图DALL·E，Anthropic的Claude全系列（Claude 3.5 Sonnet、Claude Haiku），Google的Gemini 2.5 Pro，以及DeepSeek-R1满血版——总共500+模型，都在一个API key下管理和调用。

如果你做项目需要对比不同模型的效果，[千聚api聚合站](https://www.qianjuai.com/)切换模型就是改一行参数的事情，不用注册一堆海外账户。

还有一个免费子站`free.yunwu.ai`，用GitHub账号登录就能拿到API key，每天有GPT-4o和GPT-4o-mini的免费调用额度。先跑通接入流程、验证代码能不能正常跑——这些不需要花钱。

---

## 稳定性和可靠性怎么样

[千聚api聚合站](https://www.qianjuai.com/)标称可用性99.9%，覆盖全球七大地区节点（美国、日本、韩国、英国、香港、菲律宾、俄罗斯），企业高速链，无路由二次数据留存。API key余额永不过期，支持100%保值换绑。服务已有20万+用户和800+中转代理合作伙伴。

对于需要长期稳定使用o3模型进行开发和测试的人来说，这几个数据就是硬底气。

👉 [注册千聚api聚合站，立即使用o3 + 10个Python示例脚本](https://www.qianjuai.com/register)

## 总结

1元买1美元Token、国内直连、一行代码接入o3、最低1元起充、新用户免费额度——这些组合在一起，[千聚api聚合站](https://www.qianjuai.com/)在这个方向里算是诚意十足的选择。上面给的10个Python示例脚本，复制即用，覆盖了o3-mini从对话到推理、从代码到图像的全场景。

不是它铺天盖地宣传自己有多牛，而是该有的都有，用起来不折腾，定价透明，对绝大多数开发者来说够用而且实惠。如果你正在为接入o3模型或部署o3应用发愁，这篇文章的10个示例脚本应该能帮你省下不少时间。